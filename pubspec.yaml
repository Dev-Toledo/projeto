name: projeto
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  device_preview: ^1.2.0
  crypto: ^3.0.1  # Adiciona a dependência do pacote 'crypto'
  intl: any
  provider: 6.1.2
  shared_preferences: ^2.0.6
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.11
  fl_chart: any
  sqflite: ^2.3.3
  path: ^1.9.0
  
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/
