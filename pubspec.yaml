name: projeto
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0  # Adiciona a dependência do pacote path
  crypto: ^3.0.1  # Adiciona a dependência do pacote 'crypto'
  device_preview: ^1.2.0
  intl: any
  provider: 6.1.2
  shared_preferences: ^2.0.6
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.2
  sqflite: ^2.0.0+3
  fl_chart: any
  drift: ^2.0.1
  drift_sqflite: ^1.0.0 # Se for usar em conjunto com o sqflite

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/
